<?php
use Drupal\drupal_realestate_core\Helpers\Config;

function drupal_realestate_core_node_update($node) {
  if ($node->getType() === 'homepage') {
    $homepage_uri = $node->toUrl()->toString();

    Config::setHomePage($homepage_uri);
  }
}
